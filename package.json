{
  "name": "reactivist",
  "scripts": {
    "build": "next build && babel bin/ -d dist/bin/ && babel src/ -d dist/src/",
    "start": "NODE_ENV=production node dist/bin/start.js",
    "dev": "NODE_ENV=development nodemon bin/start.js --exec babel-node --watch bin/start.js --watch app.js",
    "test": "jest --notify",
    "deploy": "now && now alias"
  },
  "dependencies": {
    "body-parser": "^1.17.2",
    "express": "^4.15.4",
    "next": "^3.2.1",
    "react": "^15.6.1",
    "react-dom": "^15.6.1",
    "republic": "^0.3.2"
  },
  "devDependencies": {
    "babel-cli": "^6.26.0",
    "babel-plugin-transform-runtime": "^6.23.0",
    "babel-preset-es2015": "^6.24.1",
    "babel-preset-react": "^6.24.1",
    "babel-preset-stage-2": "^6.24.1",
    "nodemon": "^1.11.0",
    "now": "^7.4.0"
  },
  "babel": {
    "plugins": [
      "transform-runtime"
    ],
    "presets": [
      "es2015",
      "stage-2",
      "react"
    ]
  },
  "now": {
    "alias": "reactivist.co"
  }
}
